import { ProfileSettings } from "@/components/profile/ProfileSettings";

export const metadata = {
  title: "Student Profile & Notification Preferences | GTU All In One",
  description: "Configure your Gujarat Technological University enrollment profile, branch, semester, and result push notifications.",
};

export default function ProfilePage() {
  return <ProfileSettings />;
}
